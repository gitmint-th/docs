### Thai / ภาษาไทย

สร้าง skeleton สำหรับการแปลภาษาไทย (`th-TH`) โดยยังคงหัวข้อหลัก (titles) ไว้เป็นเวอร์ชันดั้งเดิมเพื่อรักษาความเป็นมืออาชีพ. ไฟล์ที่เพิ่ม: `th-TH/index.mdx`, `th-TH/get_started/first_generation.mdx`, `th-TH/installation/system_requirements.mdx`, `th-TH/interface/overview.mdx`.

วัตถุประสงค์: ให้ reviewer ตรวจสอบโครงสร้างโฟลเดอร์, การตั้งชื่อไฟล์, และผลกระทบต่อ navigation ก่อนเริ่มแปลจริง.

ขอความช่วยเหลือจาก reviewer:
- ตรวจสอบว่าการเพิ่มโฟลเดอร์ `th-TH/` จะไม่กระทบการ build หรือเมนู (ควรเป็น safe-add).
- ให้คำแนะนำหากควรเพิ่ม `th` ใน `docs.json` ตอนนี้หรือรอจนแปลมากขึ้น.

ถัดไป: ผมสามารถแปลตัวอย่างหน้า 1-3 เป็นภาษาไทยให้ดูเป็นตัวอย่างหลังจาก PR นี้ถูก reviewed.

---

### English

Adds a Thai localization skeleton under `th-TH/` while keeping main headings/titles in the original language to preserve a professional appearance. Added files: `th-TH/index.mdx`, `th-TH/get_started/first_generation.mdx`, `th-TH/installation/system_requirements.mdx`, `th-TH/interface/overview.mdx`.

Purpose: Get early feedback on folder structure, filenames, and any navigation/build concerns before translating content at scale.

Requests for reviewers:
- Please confirm adding `th-TH/` is safe for the site build and navigation.
- Advise whether we should add a `th` language block to `docs.json` now or wait until we have more translated pages.

Next steps: I can translate 1-3 sample pages after this PR is reviewed.
